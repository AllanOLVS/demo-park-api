# H2 CONFIG
# JPA
# Imprime no log todas as instruções (insert, delete, show)
spring.jpa.show-sql=true
# Formatar as instruções sql que sÃ£o mostradas no log
spring.jpa.properties.hibernate.format_sql=false